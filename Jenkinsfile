pipeline {
    agent any
    
    stages {
        stage('Test Command') {
            steps {
                bat 'echo Hello World'
            }
        }
        // Add more stages as per your deployment or build steps
    }
}
